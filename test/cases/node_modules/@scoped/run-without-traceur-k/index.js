// Traceur auto-strictifies any code it takes in, so if this file is Traceurified the result will be `undefined`.
var globalThis = function () { return this; }();
if (globalThis !== global) {
    throw new Error("Traceur wrapping detected in a package that was not marked for Traceur Runner to process");
}

module.exports = "k";
